import Sprite from '../Sprite.js'

const Hero = class extends Sprite {
  constructor(options){
    super(options)

    const {
      name,
      health
    } = options
  }

  healthChange() {
    
  }
}